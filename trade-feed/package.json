{
  "name": "@finos/trade-feed",
  "version": "1.0.0",
  "description": "Simple SocketIO based publish-subscribe server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --require ./tracing.js index.js",
    "sbom": "node -e \"require('fs').mkdirSync('build/reports',{recursive:true})\" && cyclonedx-npm --ignore-npm-errors --output-file build/reports/sbom.json --output-format JSON",
    "sbom:xml": "node -e \"require('fs').mkdirSync('build/reports',{recursive:true})\" && cyclonedx-npm --ignore-npm-errors --output-file build/reports/sbom.xml --output-format XML"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "npm-check-updates": "^17.1.16",
    "socket.io": "^4.8.1",
    "winston": "^3.17.0",
    "@opentelemetry/sdk-node": "^0.57.0",
    "@opentelemetry/auto-instrumentations-node": "^0.54.0",
    "@opentelemetry/exporter-trace-otlp-grpc": "^0.57.0",
    "@opentelemetry/resources": "^1.30.0",
    "@opentelemetry/semantic-conventions": "^1.28.0"
  },
    "devDependencies": {
      "@cyclonedx/cyclonedx-npm": "^4.1.2"
    }
}
